"""API module registry package (Phase 3).

Holds the explicit list of application modules and their router mounts and
workers. See modules/registry.py. This is a hand-maintained registry, not a
plugin-discovery framework — modules are listed explicitly, never scanned.
"""
