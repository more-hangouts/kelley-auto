# Deprecated Scaffold

This `reliable-cars/` subfolder is a legacy scaffold and is **not** the production app.

Use the repository root as the source of truth:

- Main docs and runbook: `../README.md`
- App code: `../src`
- Payload config: `../src/payload.config.ts`

Do not deploy from this subfolder.
